sources:
  - name: NASANeo
    handler:
      jsonSchema:
        endpoint: https://api.nasa.gov
        operations:
          - type: Query
            field: getNeoFeed
            path: /neo/rest/v1/feed?start_date={arg.start_date}&end_date={arg.end_date}&api_key={arg.api_key}
            method: GET
            responseSchema: ./data/nasa-response.schema.json
            argTypeMap:
              start_date:
                type: string
              end_date:
                type: string
              api_key: 
              type: string

documents:
  - ./src/neo/operations.graphql